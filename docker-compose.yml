version: '3.9'

services:
  mysql:
    image: eu.gcr.io/serlo-shared/serlo-mysql-database:prerelease-delete-and-migrate-solutions
    platform: linux/x86_64
    pull_policy: always
    ports:
      - '3306:3306'
